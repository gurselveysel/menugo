import {timingSafeEqual} from 'node:crypto';import {createClient} from '@supabase/supabase-js';
export const dynamic='force-dynamic';export const runtime='nodejs';
export async function GET(req:Request){const secret=process.env.CRON_SECRET;const supplied=req.headers.get('authorization')??'';if(!secret||secret.length<32)return Response.json({error:'CRON_NOT_CONFIGURED'},{status:503});const a=Buffer.from(supplied),b=Buffer.from('Bearer '+secret);if(a.length!==b.length||!timingSafeEqual(a,b))return Response.json({error:'UNAUTHORIZED'},{status:401});
 try{const key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!key)return Response.json({error:'EVALUATOR_USES_DATABASE_CRON'},{status:503});const s=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,key,{auth:{persistSession:false}});const {data,error}=await s.schema('ops').rpc('crm_enqueue');if(error)throw error;return Response.json(data,{headers:{'Cache-Control':'no-store'}});}catch{return Response.json({error:'EVALUATION_FAILED'},{status:503});}}
