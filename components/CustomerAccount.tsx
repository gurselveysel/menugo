 'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';import {api,explain,label} from './transport';import {getBrowserClient} from '@/lib/supabase/browser';
export function CustomerAccount(){const[visits,setVisits]=useState<any[]|null>(null);const[error,setError]=useState('');useEffect(()=>{let alive=true;api('/api/merchant/visits').then(v=>{if(alive)setVisits(v);}).catch(e=>{if(alive)setError(explain(e));});return()=>{alive=false;};},[]);
 return <div className="customer-account"><header><Link href="/bahcesehir"><img src="/media/sariyer-brand.avif" width="600" height="200" alt="Meşhur Sarıyer Börekçisi Sandviç"/></Link><Link className="btn" href="/bahcesehir">Menüye dön</Link></header><main><p className="eyebrow">BAHÇEŞEHİR</p><h1>Hesabım</h1><p>Masa ziyaretleriniz ve siparişleriniz.</p>{error?<section className="card"><p role="status">{error}</p><Link className="btn primary" href="/giris?role=customer">Giriş yap / Hesap oluştur</Link></section>:visits===null?<p>Yükleniyor…</p>:visits.length?visits.map(v=><section className="card" key={v.id}><h2>{v.table||'Ziyaret'}</h2><p>{new Date(v.createdAt).toLocaleString('tr-TR')} · {v.orderCount} sipariş</p><span className="tag">{label(v.status)}</span>{['open','checkout'].includes(v.status)&&<Link className="btn primary" href={'/siparis?check='+v.id}>Masamı aç →</Link>}</section>):<section className="card"><h2>Henüz masa ziyaretiniz yok</h2><p>İşletmedeyken personelimizin verdiği masa QR kodunu okutun.</p><Link className="btn primary" href="/bahcesehir">Menüyü keşfet</Link></section>}{visits!==null&&<button className="text-button" onClick={async()=>{await getBrowserClient().auth.signOut();location.assign('/giris?role=customer');}}>Güvenli çıkış</button>}</main><footer><a href="https://www.menugo.app/"><img src="/media/menugo.png" width="560" height="147" alt="MenüGO — Yeni Nesil Dijital Menü"/></a></footer></div>;}
