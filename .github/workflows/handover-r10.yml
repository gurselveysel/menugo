name: Live service handover acceptance
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['.github/workflows/handover-r10.yml']
permissions:
  contents: read
jobs:
  live-handover:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci --no-audit --no-fund
      - name: Live customer pages and protected operation boundaries
        run: node tests/handover-live.integration.mjs
      - name: Live transparent logos and white merchant footer
        run: node tests/branding-ui.integration.mjs
        env:
          BRAND_TEST_BASE_URL: https://sariyerborekcisi.menugo.app
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: menugo-live-service-handover
          path: |
            public/qa/handover-live
            public/qa/branding-live
          retention-days: 3
          if-no-files-found: ignore
